# neural-storyteller
Neural Storyteller is an AI-powered image captioning system that automatically generates natural language descriptions for images. The project combines a ResNet50 visual encoder with an LSTM sequence decoder
